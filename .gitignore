# Root .gitignore for the DotConnects monorepo.
# Each app also has its own .gitignore, which still applies to its folder —
# gitignore patterns are relative to the file that declares them.

node_modules

# Next.js (carrier)
.next
out
next-env.d.ts

# SvelteKit (dotconnects-app)
.svelte-kit

# Both
.vercel
build
dist
*.tsbuildinfo

# Env — never commit real values
.env
.env.*
!.env.example

# Editors / OS
.idea
.vscode
.DS_Store
Thumbs.db
*.swp

# Logs
*.log
npm-debug.log*
